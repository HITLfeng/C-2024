#include <stdio.h>

typedef char uint8_t;

uint8_t isEnd = 1;

int main() {
    char cmd[1024];
    while (isEnd != 0)
    {
        printf(">>>>>>: ");
        scanf(%s, cmd);
    }
    
    return 0;
}